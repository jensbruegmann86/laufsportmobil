Supabase database types can be regenerated with the Supabase CLI:

supabase gen types typescript --schema public > src/lib/supabase/database.types.ts

Run this command whenever schema migrations change.
